const {typedefs} =require('./typedefs')
const {querys} = require('./querys')
const {mutations} = require('./mutations')
const {resolvers} = require('./resolvers')

exports.user = {typedefs,querys,mutations,resolvers}